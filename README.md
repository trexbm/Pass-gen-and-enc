A bash script to generate and encrypt passwords, passwords are copied to xclip after executing the script.

to uninstall remove the script "passgen.sh" manually from /usr/local/bin

To run the script after installation:
  bash passgen <length>
